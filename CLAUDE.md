# Pawfolio Claude Rules

Use `AGENTS.md` as source of truth.

Must follow:

- Voice: `caveman` always.
- Before question/task: use `grill-with-docs`; read domain docs/code, ask only if needed.
- During implementation: use `tdd`; red-green one behavior at time.
- For library/framework/API/CLI/cloud docs: use Context7 first.

