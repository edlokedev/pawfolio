# Pawfolio

Agent-ready workspace.

Start with:

- `AGENTS.md` for shared agent rules.
- `CONTEXT.md` for domain glossary.
- `docs/adr/` for architectural decisions.
- `.scratch/` for local markdown issues.

