# Issue Tracker

Use local markdown.

- Issues live under `.scratch/`.
- One issue per markdown file.
- Keep issue text self-contained: goal, context, acceptance criteria, test notes.
- Use `ready-for-agent` only when task is clear enough for unattended agent work.

If repo later moves to GitHub/GitLab/Linear/Jira, update this file and `AGENTS.md`.

